{
  "name": "elog-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "packageManager": "pnpm@11.1.2",
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.9.4",
    "turbo": "^2.10.2"
  },
  "lint-staged": {
    "*.{ts,js,json,md}": "prettier --write"
  },
  "scripts": {
    "build": "turbo build",
    "test": "turbo test --filter=!@elog/e2e",
    "changeset": "changeset",
    "version": "changeset version",
    "publish": "changeset publish",
    "prepare": "husky",
    "e2e": "pnpm --filter @elog/e2e e2e",
    "e2e:cli": "pnpm --filter @elog/e2e test:cli",
    "e2e:yuque-local": "pnpm --filter @elog/e2e test:yuque-local"
  }
}
