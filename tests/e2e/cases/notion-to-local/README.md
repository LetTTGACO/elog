# notion-to-local

End-to-end case for Notion source download, optional image transform, and local
filesystem deploy.

Required env:

- `ELOG_E2E_NOTION_TOKEN`
- `ELOG_E2E_NOTION_DATA_SOURCE_ID`

Manual path and image settings live in `elog.config.ts` under `e2eProfile`.
